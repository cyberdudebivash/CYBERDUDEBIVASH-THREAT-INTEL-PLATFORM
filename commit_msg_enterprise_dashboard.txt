feat(dashboard): production-grade Enterprise Dashboard rebuild v143.0.0

Complete rewrite of dashboard/enterprise_dashboard.html
Previous state: empty metrics cards, no data loading, broken emojis, v134.0.0
New state: fully functional, live-data, paginated, authenticated intelligence dashboard

════════════════════════════════════════════════════
CORE FIXES
════════════════════════════════════════════════════

DATA LOADING — was: silently failing relative-path fetches
  - Primary source: /api/feed (Enterprise JWT auth → full 310+ item feed)
  - Fallback 1: /api/preview (public, 20 items, no auth required)
  - Fallback 2: ../data/stix/feed_manifest.json (local static)
  - Fallback 3: GitHub raw feed_manifest.json
  - Per-source AbortController timeout (8–12s) — never hangs
  - Feed source + item count displayed in status bar

AUTH FLOW — new enterprise key unlock banner
  - API key input → POST /api/auth/token → JWT exchange
  - JWT stored in localStorage (cdb_jwt), auto-restored on reload
  - Authenticated requests use Bearer JWT header
  - Key validation feedback (tier, partial key display)
  - "Clear Key" button to revoke session

VERSION — updated throughout: v134.0.0 → v143.0.0 GOD-MODE

BROKEN EMOJI — fixed all UTF-8 mangled chars (ðŸ"" → &#128276; etc)
  All icons now use HTML entity codes for cross-platform reliability

════════════════════════════════════════════════════
PRODUCTION FEATURES ADDED
════════════════════════════════════════════════════

INTELLIGENCE TABLE
  - Paginated: 25 rows/page with Prev/Next + page counter
  - Columns: Advisory, Risk, Actor (resolved), MITRE Tactics, IOCs, KEV, Actions
  - Animated counter roll-up for all 6 metric cards
  - Per-row click → slide-in Detail Drawer (full advisory + IOC grid)
  - Export per-advisory to JSON from drawer
  - Single-row "Watch" button → add to watchlist

DETAIL DRAWER
  - Slides in from right, dark overlay backdrop
  - Shows: risk score, description, actor, MITRE tactics, IOC grid (by type)
  - IOC gate: prompts upgrade if locked (Pro tier required)
  - Actions: Add to Watchlist, Export JSON, Premium Report link

REAL-TIME SEARCH + FILTERS
  - Live filtering on all fields via JSON.stringify search
  - Severity filter pills: All / Critical / High / Medium / Low
  - Filters reset to page 1 on change

EXPORTS (all work client-side)
  - JSON: full filtered dataset
  - CSV: proper RFC-4180 quoting with all fields
  - STIX 2.1: valid bundle with indicator objects, confidence, labels

ALERTS PANEL
  - Alert count badge on nav tab (red pill)
  - Dismiss individual alerts (×) or clear all
  - Auto-fires on data load via checkWatchlistAlerts()

WATCHLISTS
  - Full CRUD: create, delete watchlists
  - Add/remove keywords inline (click tag to remove)
  - Enter key support in keyword input
  - Alerts generated when keywords match feed items (risk ≥ 6)

SAVED SEARCHES
  - Save any search + filter state
  - Click to restore: populates search box, applies filter, navigates to Intelligence
  - Delete individual, clear all

SLA MONITOR PANEL (new tab)
  - Fetches /api/sla/status (public endpoint)
  - 4 KPI cards: Uptime %, Status, Incidents (30d), Enterprise Target
  - Component health table with color-coded dots
  - Credit policy display: 10%/day breach, up to 30% of monthly fee
  - Header SLA dot indicator (green=operational, red=degraded)

API CONSOLE (fully live, not mocked)
  - Hits actual Worker endpoints at intel.cyberdudebivash.com
  - Grouped by Public / Authenticated
  - Response time + HTTP status displayed
  - API Reference table: 18 endpoints with tier labels
  - Click endpoint row → auto-selects in console
  - Auto-populates key from session if available

AUTO-REFRESH
  - setInterval: refreshData() every 5 minutes
  - Manual refresh button in status bar

TOAST NOTIFICATIONS
  - Non-blocking bottom-right toasts for all user actions
  - Success (green) and error (red) variants

════════════════════════════════════════════════════
VALIDATION
════════════════════════════════════════════════════

  HTML parser: PASSED ✅
  No broken UTF-8 encoding: PASSED ✅
  All JS functions present: PASSED ✅
  Size: 68,609 bytes, 1,194 lines ✅
  verify_p0.py: ALL 6 PATCHES VERIFIED ✅
