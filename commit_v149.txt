fix(P0): resolve SyntaxError — renderCards missing catch + orphaned catch in search map (v149.0)

ROOT CAUSE: Two JavaScript syntax errors prevented ALL 507KB script from executing.

BUG 1 (renderCards line ~8521):
  try block opened for per-card error isolation had no matching catch/finally.
  SyntaxError: Missing catch or finally after try — at }).join('') closing the map.

BUG 2 (search map line ~12470):
  Orphaned catch(_cdbCardErr) block with no preceding try inside .map(r => {}).
  Caused: missing ) after argument list syntax error.

FIX 1: Inserted '} catch(e) { return ""; }' before }).join('') in renderCards().
FIX 2: Removed orphaned catch block from search results map lambda.

IMPACT: Both syntax errors caused zero JS execution from main script block.
bootFromEmbeddedCache, renderCards, loadGOCIntel all undefined at runtime.
Dashboard showed only static HTML spinner indefinitely.

VERIFICATION: node --check passes cleanly on patched 505KB extracted script.
