--- a/index.html
+++ b/index.html
@@ -2282,7 +2282,12 @@
             updateCounter('m-total-iocs', totalIOCs);

             // v26.0 FIX: Read NEWEST entry (index 0) - manifest is sorted newest-first
-            const lastTs = data.length ? timeSince(data[data.length - 1].timestamp || data[0].timestamp) : '—';
+            // BEFORE (BUGGY): data[data.length - 1] reads OLDEST entry!
+            // AFTER (FIXED): data[0] reads NEWEST entry
+            let sortedData = [...data].sort((a, b) =>
+                new Date(b.timestamp || 0) - new Date(a.timestamp || 0)
+            );
+            const lastTs = sortedData.length ? timeSince(sortedData[0].timestamp) : '—';

             // Update sync status
             document.getElementById('m-last-sync').textContent = lastTs;
