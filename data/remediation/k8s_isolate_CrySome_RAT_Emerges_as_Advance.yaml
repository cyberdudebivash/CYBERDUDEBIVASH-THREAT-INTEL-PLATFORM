apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: apex-quarantine-4588825f8f80
  namespace: production
  labels:
    managed-by: cdb-apex-soar
    threat-ref: "CrySome RAT Emerges as Advanced .NET Malware With AV Killer and HVNC Capabilities"
spec:
  podSelector:
    matchLabels:
      apex-isolated: "true"
  policyTypes:
  - Ingress
  - Egress
  # Default Deny All to isolate the compromised pod milliseconds after detection
