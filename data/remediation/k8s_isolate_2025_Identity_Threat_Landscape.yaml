apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: apex-quarantine-96322dd585bb
  namespace: production
  labels:
    managed-by: cdb-apex-soar
    threat-ref: "2025 Identity Threat Landscape Report: Inside the Infostealer Economy: Credential Threats in 2025"
spec:
  podSelector:
    matchLabels:
      apex-isolated: "true"
  policyTypes:
  - Ingress
  - Egress
  # Default Deny All to isolate the compromised pod milliseconds after detection
