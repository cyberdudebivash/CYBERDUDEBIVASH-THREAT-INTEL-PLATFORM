apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: apex-quarantine-30eb9465900a
  namespace: production
  labels:
    managed-by: cdb-apex-soar
    threat-ref: "Securing the Supply Chain: How SentinelOne®’s AI EDR Stops the Axios Attack Autonomously"
spec:
  podSelector:
    matchLabels:
      apex-isolated: "true"
  policyTypes:
  - Ingress
  - Egress
  # Default Deny All to isolate the compromised pod milliseconds after detection
