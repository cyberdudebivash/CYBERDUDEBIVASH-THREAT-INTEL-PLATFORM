apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: apex-quarantine-77afdc21f823
  namespace: production
  labels:
    managed-by: cdb-apex-soar
    threat-ref: "Apple Patches (almost) everything again. March 2026 edition., (Wed, Mar 25th)"
spec:
  podSelector:
    matchLabels:
      apex-isolated: "true"
  policyTypes:
  - Ingress
  - Egress
  # Default Deny All to isolate the compromised pod milliseconds after detection
