apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: apex-quarantine-441bf50cf1cd
  namespace: production
  labels:
    managed-by: cdb-apex-soar
    threat-ref: "Dell Wyse Management Vulnerabilities Enables Complete System Compromise"
spec:
  podSelector:
    matchLabels:
      apex-isolated: "true"
  policyTypes:
  - Ingress
  - Egress
  # Default Deny All to isolate the compromised pod milliseconds after detection
