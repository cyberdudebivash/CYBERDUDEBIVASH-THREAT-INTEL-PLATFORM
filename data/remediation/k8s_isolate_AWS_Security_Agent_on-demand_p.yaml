apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: apex-quarantine-d9ebd829b8ce
  namespace: production
  labels:
    managed-by: cdb-apex-soar
    threat-ref: "AWS Security Agent on-demand penetration testing now generally available"
spec:
  podSelector:
    matchLabels:
      apex-isolated: "true"
  policyTypes:
  - Ingress
  - Egress
  # Default Deny All to isolate the compromised pod milliseconds after detection
