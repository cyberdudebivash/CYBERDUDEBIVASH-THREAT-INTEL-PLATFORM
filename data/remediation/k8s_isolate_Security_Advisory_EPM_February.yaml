apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: apex-quarantine-a84d11f56b46
  namespace: production
  labels:
    managed-by: cdb-apex-soar
    threat-ref: "Security Advisory EPM February 2026 for EPM 2024"
spec:
  podSelector:
    matchLabels:
      apex-isolated: "true"
  policyTypes:
  - Ingress
  - Egress
  # Default Deny All to isolate the compromised pod milliseconds after detection
