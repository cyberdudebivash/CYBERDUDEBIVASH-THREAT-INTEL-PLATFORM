apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: apex-quarantine-8098772deca3
  namespace: production
  labels:
    managed-by: cdb-apex-soar
    threat-ref: "BeatBanker: A dual‑mode Android Trojan"
spec:
  podSelector:
    matchLabels:
      apex-isolated: "true"
  policyTypes:
  - Ingress
  - Egress
  # Default Deny All to isolate the compromised pod milliseconds after detection
