apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: apex-quarantine-312cb691299b
  namespace: production
  labels:
    managed-by: cdb-apex-soar
    threat-ref: "Official Launch: CYBERDUDEBIVASH® CyberTwin v1.0 Building a Native Windows Exposure Scanner with Rust"
spec:
  podSelector:
    matchLabels:
      apex-isolated: "true"
  policyTypes:
  - Ingress
  - Egress
  # Default Deny All to isolate the compromised pod milliseconds after detection
