apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: apex-quarantine-f23dfad13f66
  namespace: production
  labels:
    managed-by: cdb-apex-soar
    threat-ref: "Industrialization of the Fraud Ecosystem Blog"
spec:
  podSelector:
    matchLabels:
      apex-isolated: "true"
  policyTypes:
  - Ingress
  - Egress
  # Default Deny All to isolate the compromised pod milliseconds after detection
