apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: apex-quarantine-4ff66e4c0359
  namespace: production
  labels:
    managed-by: cdb-apex-soar
    threat-ref: "Chrome security flaw enabled spying via Gemini Live assistant"
spec:
  podSelector:
    matchLabels:
      apex-isolated: "true"
  policyTypes:
  - Ingress
  - Egress
  # Default Deny All to isolate the compromised pod milliseconds after detection
