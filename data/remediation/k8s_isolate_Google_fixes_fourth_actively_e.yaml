apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: apex-quarantine-235d935d9b93
  namespace: production
  labels:
    managed-by: cdb-apex-soar
    threat-ref: "Google fixes fourth actively exploited Chrome zero-day of 2026"
spec:
  podSelector:
    matchLabels:
      apex-isolated: "true"
  policyTypes:
  - Ingress
  - Egress
  # Default Deny All to isolate the compromised pod milliseconds after detection
