apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: apex-quarantine-9340b474b9ae
  namespace: production
  labels:
    managed-by: cdb-apex-soar
    threat-ref: "Dark Web Intercept: Telegram_RaaS_Syndicate - Potential Threat Activity"
spec:
  podSelector:
    matchLabels:
      apex-isolated: "true"
  policyTypes:
  - Ingress
  - Egress
  # Default Deny All to isolate the compromised pod milliseconds after detection
