apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: apex-quarantine-7b5592ccb972
  namespace: production
  labels:
    managed-by: cdb-apex-soar
    threat-ref: "AI-augmented threat actor accesses FortiGate devices at scale"
spec:
  podSelector:
    matchLabels:
      apex-isolated: "true"
  policyTypes:
  - Ingress
  - Egress
  # Default Deny All to isolate the compromised pod milliseconds after detection
