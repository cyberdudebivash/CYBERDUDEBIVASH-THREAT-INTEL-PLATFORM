apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: apex-quarantine-8924db564ffb
  namespace: production
  labels:
    managed-by: cdb-apex-soar
    threat-ref: "Firefox 149 Released With Patch for 37 Vulnerabilities that Enables Remote Attacks"
spec:
  podSelector:
    matchLabels:
      apex-isolated: "true"
  policyTypes:
  - Ingress
  - Egress
  # Default Deny All to isolate the compromised pod milliseconds after detection
