apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: apex-quarantine-1d4223eeff9a
  namespace: production
  labels:
    managed-by: cdb-apex-soar
    threat-ref: "2025: The Untold Stories of Check Point Research"
spec:
  podSelector:
    matchLabels:
      apex-isolated: "true"
  policyTypes:
  - Ingress
  - Egress
  # Default Deny All to isolate the compromised pod milliseconds after detection
