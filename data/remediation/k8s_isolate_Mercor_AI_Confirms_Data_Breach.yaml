apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: apex-quarantine-29daeb129c45
  namespace: production
  labels:
    managed-by: cdb-apex-soar
    threat-ref: "Mercor AI Confirms Data Breach Following Lapsus$ Claims of 4TB Data Theft"
spec:
  podSelector:
    matchLabels:
      apex-isolated: "true"
  policyTypes:
  - Ingress
  - Egress
  # Default Deny All to isolate the compromised pod milliseconds after detection
