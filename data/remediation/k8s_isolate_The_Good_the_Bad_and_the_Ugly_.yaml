apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: apex-quarantine-49aecdda3f13
  namespace: production
  labels:
    managed-by: cdb-apex-soar
    threat-ref: "The Good, the Bad and the Ugly in Cybersecurity – Week 14"
spec:
  podSelector:
    matchLabels:
      apex-isolated: "true"
  policyTypes:
  - Ingress
  - Egress
  # Default Deny All to isolate the compromised pod milliseconds after detection
