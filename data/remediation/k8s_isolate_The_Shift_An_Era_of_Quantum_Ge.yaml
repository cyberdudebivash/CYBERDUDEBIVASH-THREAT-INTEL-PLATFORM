apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: apex-quarantine-d94306801d7a
  namespace: production
  labels:
    managed-by: cdb-apex-soar
    threat-ref: "The Shift: An Era of Quantum Geopolitics"
spec:
  podSelector:
    matchLabels:
      apex-isolated: "true"
  policyTypes:
  - Ingress
  - Egress
  # Default Deny All to isolate the compromised pod milliseconds after detection
