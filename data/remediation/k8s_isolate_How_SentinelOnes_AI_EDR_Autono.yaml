apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: apex-quarantine-7345b03fd30b
  namespace: production
  labels:
    managed-by: cdb-apex-soar
    threat-ref: "How SentinelOne's AI EDR Autonomously Discovered and Stopped Anthropic's Claude from Executing a Zero Day Supply Chain Attack, Globally"
spec:
  podSelector:
    matchLabels:
      apex-isolated: "true"
  policyTypes:
  - Ingress
  - Egress
  # Default Deny All to isolate the compromised pod milliseconds after detection
