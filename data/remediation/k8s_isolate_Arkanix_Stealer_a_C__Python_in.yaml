apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: apex-quarantine-3984dd5324ec
  namespace: production
  labels:
    managed-by: cdb-apex-soar
    threat-ref: "Arkanix Stealer: a C++ & Python infostealer"
spec:
  podSelector:
    matchLabels:
      apex-isolated: "true"
  policyTypes:
  - Ingress
  - Egress
  # Default Deny All to isolate the compromised pod milliseconds after detection
