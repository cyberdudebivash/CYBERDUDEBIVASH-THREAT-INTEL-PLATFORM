apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: apex-quarantine-db410ed3a23b
  namespace: production
  labels:
    managed-by: cdb-apex-soar
    threat-ref: "The SOC Files: Time to “Sapecar”. Unpacking a new Horabot campaign in Mexico"
spec:
  podSelector:
    matchLabels:
      apex-isolated: "true"
  policyTypes:
  - Ingress
  - Egress
  # Default Deny All to isolate the compromised pod milliseconds after detection
