apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: apex-quarantine-cbe9a7eb21b9
  namespace: production
  labels:
    managed-by: cdb-apex-soar
    threat-ref: "Spam and phishing in 2025"
spec:
  podSelector:
    matchLabels:
      apex-isolated: "true"
  policyTypes:
  - Ingress
  - Egress
  # Default Deny All to isolate the compromised pod milliseconds after detection
