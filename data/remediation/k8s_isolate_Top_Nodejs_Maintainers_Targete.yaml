apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: apex-quarantine-ccfbbc05193d
  namespace: production
  labels:
    managed-by: cdb-apex-soar
    threat-ref: "Top Node.js Maintainers Targeted in Sophisticated Social Engineering Scheme"
spec:
  podSelector:
    matchLabels:
      apex-isolated: "true"
  policyTypes:
  - Ingress
  - Egress
  # Default Deny All to isolate the compromised pod milliseconds after detection
