apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: apex-quarantine-bdc1639433e7
  namespace: production
  labels:
    managed-by: cdb-apex-soar
    threat-ref: "Microsoft launches new European Security Program"
spec:
  podSelector:
    matchLabels:
      apex-isolated: "true"
  policyTypes:
  - Ingress
  - Egress
  # Default Deny All to isolate the compromised pod milliseconds after detection
