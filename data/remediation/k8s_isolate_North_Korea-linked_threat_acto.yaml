apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: apex-quarantine-3180478b998a
  namespace: production
  labels:
    managed-by: cdb-apex-soar
    threat-ref: "North Korea-linked threat actors abuse VS Code auto-run to spread StoatWaffle malware"
spec:
  podSelector:
    matchLabels:
      apex-isolated: "true"
  policyTypes:
  - Ingress
  - Egress
  # Default Deny All to isolate the compromised pod milliseconds after detection
