apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: apex-quarantine-e3a5680c87a9
  namespace: production
  labels:
    managed-by: cdb-apex-soar
    threat-ref: "Can the Security Platform Finally Deliver for the Mid-Market?"
spec:
  podSelector:
    matchLabels:
      apex-isolated: "true"
  policyTypes:
  - Ingress
  - Egress
  # Default Deny All to isolate the compromised pod milliseconds after detection
