# =================================================================
# CYBERDUDEBIVASH® SENTINEL APEX - MASTER IGNORE FILE
# =================================================================

# --- 🛑 CRITICAL: SOVEREIGNTY & AUTH (NEVER PUSH THESE) ---
secrets/
*.pem
*.key
*.pfx
credentials/
token.json
credentials.json
.env

# --- 📦 ENTERPRISE ASSETS (PROTECT REVENUE) ---
# Do not push the raw sellable assets; let the engine upload them to Gumroad
data/rule_packs/
data/playbooks/
data/certificates/
data/enterprise_kits/
data/temp/

# --- 📉 PERSISTENCE & TRACKING ---
# Runtime data — regenerated by pipeline, not source-controlled
data/blogger_processed.json
data/sentinel_apex.db
data/manifest_audit.json
data/manifest_backups/
data/status/
*.log

# --- STIX DATA ---
# Track feed_manifest.json (single source of truth), ignore generated bundles
data/stix/*.json
!data/stix/feed_manifest.json

# --- 🐍 PYTHON HYGIENE ---
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/
venv/
env/

# --- 🖥️ OS TRASH ---
.DS_Store
Thumbs.db
