v146.1-guard-fix: update dashboard_frontend_guard.py to approve api/feed.json

STAGE 3.92 was blocking deployment with false-positive HARD FAIL.

ROOT CAUSE OF FALSE POSITIVE:
  CHECK 2 used `'feed.json' in line` substring match which catches
  'api/feed.json' — a DIFFERENT and APPROVED same-domain path.
  api/feed.json is NOT the deprecated root feed.json (which IS banned).

GUARD CHANGES (v145.0 → v146.0):
  CHECK 2: Rewritten with precise banned patterns:
    BANNED:  root feed.json | RAW_MANIFEST | raw.githubusercontent.com | jsDelivr
    APPROVED: api/feed.json (same-domain, zero CORS risk, pipeline-updated)
  CHECK 2b (NEW): api/feed.json MUST be present in MANIFEST_URLS
    Regression guard — prevents silent revert to API-only arch (the P0 root cause)
  CHECK 6 (NEW): P0 safety timer must be present
  CHECK 7 (NEW): Terminal fallback grid-clear must be present
  Total checks: 5 → 7

ARCHITECTURE RATIONALE (v146.0):
  Worker API (intel.cyberdudebivash.com) = PRIMARY source
  api/feed.json (same-domain) = FALLBACK — CORS-safe, always accessible
  CORS blocks Worker fetch in browser silently even when API responds correctly
  Removing api/feed.json → stuck loader → zero-card dashboard = P0 regression

Local validation: 7/7 CHECKS PASS | 0 ERRORS | 0 WARNINGS
