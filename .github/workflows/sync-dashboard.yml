# =============================================================================
# CYBERDUDEBIVASH(R) SENTINEL APEX v134.0.0 " DASHBOARD SYNC PIPELINE
# " DISABLED AS OF v134.0.0 " ALL FUNCTIONALITY MERGED INTO sentinel-blogger
# =============================================================================
#
# WHY THIS WORKFLOW IS DISABLED (v134.0.0 " 2026-04-18):
#
# This workflow caused the following P0 production issues:
#
#   1. MANIFEST DESTRUCTION (runs #744, #745, #748):
#      The `--force-rebuild` step re-parsed only the CURRENT run's STIX bundles
#      (e.g. 52 bundles ' 9 entries), destroying the engine's authoritative
#      2222-entry manifest. sentinel-blogger THEN uploaded this corrupted 9-entry
#      manifest to R2, and the Worker served empty data to the dashboard.
#
#   2. RACE CONDITION / OVERWRITE:
#      This workflow used the SAME concurrency group (`sentinel-data-writer`) as
#      sentinel-blogger. It queued directly behind sentinel-blogger on completion,
#      then overwrote `data/stix/feed_manifest.json` in git with the
#      --force-rebuild output (often stale or small). This invalidated
#      sentinel-blogger's authoritative commit every run.
#
#   3. STALE EMBEDDED_INTEL (run #748):
#      This workflow injected EMBEDDED_INTEL from the --force-rebuild manifest
#      (which was stale/smaller) into index.html, overwriting the fresh data
#      that sentinel-blogger had just committed. Dashboard showed "60h ago".
#
#   4. DUPLICATE OPERATIONS:
#      sentinel-blogger v134.0.0 now handles everything this workflow did:
#         EMBEDDED_INTEL injection (Step 17: update_embedded_intel.py)
#         R2 upload (Step 18: mandatory overwrite)
#         KV cache bust (Step 19)
#         git commit + gh-pages deploy (Steps 20-21)
#      Running this workflow AFTER sentinel-blogger produced duplicate commits,
#      extra push attempts, and non-fast-forward failures.
#
# HOW TO RE-ENABLE:
#   Only re-enable this workflow after removing --force-rebuild and the
#   `git add data/stix/feed_manifest.json` line. Add it to the
#   `autonomous-guardian` concurrency group (NOT sentinel-data-writer).
#   For emergency manual execution, use `workflow_dispatch` below.
#
# =============================================================================

name: "DISABLED - Dashboard Sync v134.0 (merged into sentinel-blogger v134.0)"

on:
  # " ALL AUTO-TRIGGERS DISABLED " sentinel-blogger v134.0 handles dashboard sync
  # workflow_run:              DISABLED: caused overwrite race with sentinel-blogger
  #   workflows: ["sentinel-blogger"]
  #   types: [completed]
  # schedule:                  DISABLED: caused --force-rebuild manifest destruction
  #   - cron: '20 */4 * * *'
  workflow_dispatch:          #  MANUAL EMERGENCY USE ONLY
    inputs:
      force_patch:
        description: 'Force EMBEDDED_INTEL patch (EMERGENCY USE ONLY " reads current git state)'
        type: boolean
        default: false

permissions:
  contents: write

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

concurrency:
  # " CHANGED: was sentinel-data-writer (caused race with sentinel-blogger)
  # Now uses isolated group " if manually triggered, won't conflict with main pipeline
  group: sync-dashboard-manual
  cancel-in-progress: false

jobs:
  sync-dashboard:
    name: "DISABLED - Emergency Manual EMBEDDED_INTEL Patch"
    runs-on: ubuntu-latest
    timeout-minutes: 15
    # " GATE: Only run on explicit manual dispatch with force_patch=true
    if: >-
      github.event_name == 'workflow_dispatch' &&
      github.event.inputs.force_patch == 'true'

    steps:
      - name: "  Safety Warning"
        run: |
          echo "EMERGENCY MANUAL SYNC - sync-dashboard is DISABLED in v134.0.0"
          echo "    sentinel-blogger handles all dashboard sync automatically."
          echo "    This manual run will ONLY patch EMBEDDED_INTEL from current git state."
          echo "    It will NOT run --force-rebuild (manifest destruction risk)."
          echo "    It will NOT commit data/stix/feed_manifest.json."

      - name: "Checkout Repository"
        uses: actions/checkout@v4.2.2
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 1
          ref: main

      - name: "Set up Python 3.12"
        uses: actions/setup-python@v5.4.0
        with:
          python-version: '3.12'

      - name: "Patch EMBEDDED_INTEL (emergency - NO --force-rebuild)"
        run: |
          echo "=== Emergency EMBEDDED_INTEL patch from current data/stix/feed_manifest.json ==="
          python3 -c "
          import json
          d = json.load(open('data/stix/feed_manifest.json'))
          items = d.get('advisories', d if isinstance(d, list) else [])
          print(f'Manifest: {len(items)} entries - proceeding with patch')
          "
          python3 scripts/update_embedded_intel.py && echo "... Emergency EMBEDDED_INTEL patch OK" || {
            echo " EMBEDDED_INTEL patch FAILED"
            exit 1
          }

      - name: "Safe Commit & Push (index.html ONLY)"
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config --local user.email "dashboard@cyberdudebivash.com"
          git config --local user.name  "CDB-Dashboard-Emergency-Bot"
          git remote set-url origin "https://x-access-token:${GH_TOKEN}@github.com/${{ github.repository }}"
          git rebase --abort 2>/dev/null || true
          git merge  --abort  2>/dev/null || true
          rm -rf .git/rebase-merge .git/rebase-apply 2>/dev/null || true

          # " STRICTLY index.html ONLY " do NOT commit feed_manifest.json
          git add index.html 2>/dev/null || true
          # " BLOCKED: git add -f data/stix/feed_manifest.json  manifest destruction risk

          if git diff --staged --quiet; then
            echo "No changes to commit."
            exit 0
          fi

          git commit -m "Emergency Dashboard Sync v134.0 - EMBEDDED_INTEL patch @ $(date -u +'%Y-%m-%d %H:%M UTC') [skip ci]"

          for i in 1 2 3 4; do
            git fetch origin main --quiet
            git rebase origin/main --quiet 2>/dev/null || {
              git rebase --abort 2>/dev/null || true
              git merge origin/main -X ours --no-edit --quiet || true
            }
            git push origin main && { echo "Push OK (attempt $i/4)"; break; }
            sleep $((i * 15))
            [ $i -eq 4 ] && echo "Push deferred."
          done

