# ═══════════════════════════════════════════════════════════
# CDB SENTINEL APEX ULTRA v17.0 — Dependencies
# ═══════════════════════════════════════════════════════════

# --- Core Intelligence Ingestion ---
feedparser==6.0.11

# --- GOC Institutional Reporting ---
fpdf2==2.7.8
requests==2.31.0

# --- Google GOC Authority Integration ---
google-api-python-client==2.118.0
google-auth-httplib2==0.2.0
google-auth-oauthlib==1.2.0

# --- STIX 2.1 Serialization ---
# Using built-in json/uuid for core bundles
# stix2==3.0.1  # Optional: uncomment for advanced STIX operations

# --- Forensic & Detection Utilities ---
python-dateutil==2.8.2
pyyaml==6.0.1

# --- Environment Configuration ---
python-dotenv==1.0.1

# ═══════════════════════════════════════════════════════════
# v17.0 ADDITIONS
# ═══════════════════════════════════════════════════════════
# All new modules use only Python stdlib (json, os, time, re, tarfile,
# shutil, logging, datetime, collections) — no new external packages needed.
# Optional future upgrades:
#   fastapi==0.109.0          # For serving public/enterprise API endpoints
#   uvicorn==0.27.0           # ASGI server for FastAPI
#   stix2==3.0.1              # Advanced STIX 2.1 operations


# ═══════════════════════════════════════════════════════════
# v18.0 ADDITIONS
# ═══════════════════════════════════════════════════════════
# Weekly digest email via SendGrid (optional — only needed if SENDGRID_API_KEY is set)
sendgrid==6.11.0

# Note: revenue_bridge.py, upsell_injector.py, weekly_digest.py
# use only stdlib + sendgrid (optional). No other new deps required.
