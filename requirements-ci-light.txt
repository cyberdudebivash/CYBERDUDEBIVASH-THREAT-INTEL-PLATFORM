# =============================================================================
# CYBERDUDEBIVASH(R) SENTINEL APEX
# requirements-ci-light.txt  --  LIGHTWEIGHT CI REQUIREMENTS
# Version: v148.0.0
#
# PURPOSE:
#   Installs ONLY packages required for CI pipelines that do NOT need:
#     - torch / transformers (2GB+ ML models)
#     - scipy / scikit-learn  (heavy numerical stack)
#     - Google API / OAuth    (only used by sentinel-blogger)
#     - Stripe                (only used by revenue-orchestrator)
#
#   Use this in: status-monitor, weekly-analyst-briefing, bughunter-*,
#                detection-engine, report-engine, nexus, convergence,
#                omnishield, arsenal, zerodayhunter, ai-threat-analyst,
#                ai-predictions, precognition-engine, sovereign-platform,
#                genesis-powerhouse workflows.
#
#   Use requirements.txt ONLY for sentinel-blogger (needs full ML stack).
#
# INSTALL:
#   pip install -r requirements-ci-light.txt
# =============================================================================

# --- CORE WEB & API ---
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.5.3
python-dotenv==1.0.0
python-multipart==0.0.6

# --- NETWORKING & INTEL INGESTION ---
requests==2.32.3
httpx==0.26.0
aiohttp==3.9.2
feedparser==6.0.10
beautifulsoup4==4.12.3
urllib3>=1.26.18,<2.0
charset_normalizer>=3.3.2,<4.0

# --- STIX / TAXII (threat intel formats) ---
stix2==3.0.1
stix2-validator==3.1.2
taxii2-client==2.3.0

# --- SECURITY & JWT ---
cryptography==42.0.2
pyjwt[crypto]==2.8.0

# --- LIGHT NUMERICAL (no torch/transformers/scipy) ---
numpy==1.26.3

# --- PDF & REPORT GENERATION ---
reportlab==4.0.9
fpdf2==2.7.7

# --- MONITORING ---
psutil==5.9.8
prometheus-client==0.19.0
colorama==0.4.6
