# CYBERDUDEBIVASH SENTINEL APEX — SaaS Module Dependencies (v49-v53)
# Install: pip install -r requirements_saas.txt

# API Server (v49)
fastapi>=0.109.0,<1.0
uvicorn[standard]>=0.27.0,<1.0
pydantic>=2.5.0,<3.0

# Report Engine (v52) - PDF generation
# weasyprint>=62.0  # Optional: uncomment for PDF report generation

# Existing platform dependencies (already in requirements.txt)
# requests, feedparser, stix2, etc.
